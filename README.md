# Weather Analytics Dashboard 🌦️

A full-stack Python application that logs real-time weather data to a NoSQL database and visualizes it in a custom desktop GUI.

## 🛠️ Tech Stack

- **Language:** Python 3.10+
- **Database:** MongoDB (Time-Series Data)
- **Frontend:** CustomTkinter (Modern UI)
- **API:** OpenWeatherMap

## 🚀 How to Run

1.  **Install Dependencies:**
    ```bash
    pip install -r requirements.txt
    ```
2.  **Configure:**
    Create a `.env` file with your API keys.
3.  **Start the Backend:**
    ```bash
    python backend.py
    ```
4.  **Launch the Dashboard:**
    ```bash
    python frontend.py
    ```
